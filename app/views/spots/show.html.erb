<% if @spot && @spot.status != "delete" %>  

<div class="spot-group">
  <h2> Appointment with <%= @spot.calendar.name %></h2>
  
  <div class="button">
    <%= local_time(@spot.start_date, '%d-%m-%Y at %H:%M') %> - 
    <%= local_time(@spot.end_date, '%H:%M') %>
  </div>

<div class="button--group">
  <%= link_to "Add to calendar", :class => "small--button", spot_show_path(@spot.slug, format: :ics) %>

  <a class="small--button rainbow" onclick="moreConfetti()">Just give me more confetti</a>
</div>

</div>

<br>
  <%= link_to "Cancel this appointment", spot_cancel_path(@spot.slug) %>
<% else %>
  <h2>Nothing to see here...sorry!</h2>
<% end %>

<% if @spot && @spot.status == "delete" %>  
  Sorry, spot was deleted by the owner.
<% end %>





